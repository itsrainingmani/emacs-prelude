;; (setq prelude-theme 'doom-horizon-theme)
(setq prelude-theme 'doom-horizon)
(setq prelude-minimalistic-ui t)

;; This is equivalent to
;; (global-nlinum-mode -1)
;; (menu-bar-mode -1)
;; but is better since those elements don't appear temporarily
